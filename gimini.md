# Project-Specific Instructions

- **UI Framework:** Always build components using Material UI when possible.
- **Styling:** Prefer Material UI's theme system with the `sx` prop for styling over traditional CSS files.
- **Icons:** Use icons from either Lucid React or Material Icons libraries.
